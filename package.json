{
  "name": "odds-research",
  "version": "0.1.0",
  "private": true,
  "description": "Prototype: pulls a day's games from ESPN, attaches (mock) odds and flags games where the favourite looks undervalued",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
